language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t howlvaldes/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true howlvaldes/docker-react npm run test